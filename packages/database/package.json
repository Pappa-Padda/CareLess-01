{
  "name": "@repo/database",
  "version": "0.0.1",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "push": "env-cmd -f ../../.env prisma db push",
    "generate": "env-cmd -f ../../.env prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^6.19.1"
  },
  "devDependencies": {
    "prisma": "^6.19.1",
    "typescript": "^5",
    "@types/node": "^20"
  }
}