{
  "name": "@repo/database",
  "version": "0.0.1",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "push": "prisma db push",
    "generate": "prisma generate",
    "seed": "ts-node --transpile-only prisma/seed.ts"
  },
  "dependencies": {
    "@prisma/client": "^6.19.1",
    "bcrypt": "^5.1.0"
  },
  "devDependencies": {
    "prisma": "^6.19.1",
    "typescript": "^5",
    "@types/node": "^20"
    ,"ts-node": "^10"
  }
}